{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "buildCommand": "cd backend && if [ -n \"$DATABASE_URL_UNPOOLED\" ]; then export DATABASE_URL=\"$DATABASE_URL_UNPOOLED\"; fi && if [ -n \"$PGHOST_UNPOOLED\" ]; then export PGHOST=\"$PGHOST_UNPOOLED\"; fi && python manage.py migrate --noinput && python manage.py seed_questions",
  "functions": {
    "api/index.py": {
      "excludeFiles": "{.git/**,.github/**,venv/**,node_modules/**,frontend/**,mobile/**,BlackCardShowdown/**,backend/staticfiles/**,backend/db.sqlite3,Dockerfile,.dockerignore,response.json}"
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
